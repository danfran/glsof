apply plugin: 'shadow'
sourceCompatibility = 1.7
version = '2.4.2'

jar {
    manifest {
        attributes 'Main-Class': 'net.sourceforge.glsof.filemonitor.rmi.RmiServiceImpl', 'Implementation-Title': 'Remote-Lsof', 'Implementation-Version': version
    }
}

dependencies {
    compile project(':common')
}

shadow {
    artifactAttached = false
    exclude 'META-INF/*.SF'
    exclude 'META-INF/*.DSA'
    exclude 'META-INF/*.RSA'
}